DB_URL=""
PORT=3000
ENV="dev"
ROUTING_SERVICE_ADDR="localhost:50051"
DB_TOOLS_ADDR="localhost:50053"
GEOCODING_ADDR="localhost:50051"
TRAFFIC_UPDATER_ADDR="localhost:50052"
# Optional for Firebase Admin Auth with ADC in local development.
# Use your Firebase project ID (same as GOOGLE_CLOUD_PROJECT).
FIREBASE_PROJECT_ID=""

# Optional fallback if FIREBASE_PROJECT_ID is empty.
GOOGLE_CLOUD_PROJECT=""